# cryptanalyseFileAffectRansomwares


### Before running the app , make sure you run one of these commands to install all requirements.
- pip install -r requirements.txt
- conda install -c anaconda -r requirements.txt
